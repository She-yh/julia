module.exports = {
    //打包是否生成.map文件
    // publicPath: process.env.NODE_ENV === 'production' ? "./../" : './',//服务器用
    // assetsDir: "static",
    publicPath:'./',
    productionSourceMap: false,
    lintOnSave: false,
};
